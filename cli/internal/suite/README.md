# `internal/suite` — Suite directory and binding verification (planned)

Will own SuiteDirectory verification and registry:

- `suite_directory.go`, `suite_registry.go`, `evm_suite_values.go`, `moderation_values.go`, `backend.go`

See `internal/evm/README.md` for EVM split.
