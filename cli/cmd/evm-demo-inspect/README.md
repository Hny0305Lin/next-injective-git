# Deprecated — moved to `cli/tools/evm-demo-inspect`

This demo/operator binary was reorganized to `cli/tools/evm-demo-inspect` for release clarity.

Run via: `go run ./cli/tools/evm-demo-inspect` or `go build -o /tmp/evm-demo-inspect ./cli/tools/evm-demo-inspect`
