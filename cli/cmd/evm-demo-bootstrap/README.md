# Deprecated — moved to `cli/tools/evm-demo-bootstrap`

This demo/operator binary was reorganized to `cli/tools/evm-demo-bootstrap` for release clarity.

Run via: `go run ./cli/tools/evm-demo-bootstrap` or `go build -o /tmp/evm-demo-bootstrap ./cli/tools/evm-demo-bootstrap`
