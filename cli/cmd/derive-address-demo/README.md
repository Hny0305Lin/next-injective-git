# Deprecated — moved to `cli/tools/derive-address-demo`

This demo/operator binary was reorganized to `cli/tools/derive-address-demo` for release clarity.

Run via: `go run ./cli/tools/derive-address-demo` or `go build -o /tmp/derive-address-demo ./cli/tools/derive-address-demo`
