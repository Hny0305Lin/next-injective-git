# Deprecated — moved to `cli/tools/evm-demo-deploy-rest`

This demo/operator binary was reorganized to `cli/tools/evm-demo-deploy-rest` for release clarity.

Run via: `go run ./cli/tools/evm-demo-deploy-rest` or `go build -o /tmp/evm-demo-deploy-rest ./cli/tools/evm-demo-deploy-rest`
