# `src/app` — Application shell (planned)

- `App.tsx` (from `src/App.tsx`)
- `router.tsx` — route table extraction
- `layout/` — topbar / side-nav / footer

During migration, `src/App.tsx` re-exports from new location.
