# `src/lib/chain` — Low-level chain client (planned)

Will cluster transport and registry:

- `transport.ts`, `registry.ts`, `abis.ts`, `chain.ts`, `errors.ts`

See `features/wallet` for wallet slice.
