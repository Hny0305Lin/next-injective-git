# `src/features` — Domain slices (planned)

Organize by business capability:

- `repo/` — pages/Repo + gitstore
- `explorer/` — Explorer + search
- `monitor/` — Monitor + cards
- `archive/` — Archive + cosmwasm-v1
- `wallet/` — WalletContext, wallet, walletconnect
- `ipfs/` — IpfsExplorer + ipfs-probe

Incremental: new code here; old `lib/` and `pages/` remain with re-exports until cutover.
