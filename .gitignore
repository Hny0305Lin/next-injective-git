# Rust
target/
# NOTE: contracts/*/Cargo.lock IS committed — pinned versions are required
# for Rust 1.81-compatible wasm builds (see README build requirements).

# Go
cli/bin/
*.exe

# Web
web/node_modules/
web/dist/

# logs / scratch
*.log

# IDE / OS
.vscode/
.idea/
.qoder/
Thumbs.db
.DS_Store

# scratch / dependency-inspection scripts (never committed)
web/check-*.sh
web/derive-addr.mjs
scripts/tmp-*.sh
scripts/probe-gateways.sh

# TS incremental build cache
web/tsconfig.tsbuildinfo

# Vercel CLI (deploy scripts, logs, project link)
web/.vercel-tmp/
web/.vercel/
.vercel/
